import CustomForm from "./CustomForm";

const CustomFormContainer = ({ formInfo, inputsInfo }) => {
  return <CustomForm formInfo={formInfo} inputsInfo={inputsInfo} />;
};

export default CustomFormContainer;
